services:
  - type: web
    name: feranime-resolver
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run resolver
    healthCheckPath: /health
    autoDeploy: false
    envVars:
      - key: NODE_ENV
        value: production
